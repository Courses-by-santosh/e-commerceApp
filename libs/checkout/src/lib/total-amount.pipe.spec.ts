import { TotalAmountPipe } from './total-amount.pipe';

describe('TotalAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
